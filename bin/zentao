#!/bin/bash

zentaoDir=$0
nodebin=node

if [ -f "/usr/bin/zentao" ]; then
	script=`ls -l /usr/bin/zentao`
	zentaoDir=`dirname ${script#*->}`
else
	zentaoDir=`dirname $0`
fi

$nodebin $zentaoDir/app.js $@
